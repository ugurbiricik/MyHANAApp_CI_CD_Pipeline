{
  "name": "ui5.walkthrough",
  "version": "1.0.0",
  "private": true,
  "author": "SAP SE",
  "description": "UI5 Demo App - Walkthrough Tutorial",
  "scripts": {
    "build": "ui5 build --config=ui5-deploy.yaml --all --clean-dest",
    "start": "ui5 serve --config=ui5-local.yaml",
    "serve-dist": "ws --compress -d dist --open"
  },
  "devDependencies": {
    "@ui5/cli": "^4",
    "local-web-server": "^5",
    "ui5-middleware-simpleproxy": "^3"
  }
}